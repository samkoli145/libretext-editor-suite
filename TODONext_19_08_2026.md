/**

- ═══════════════════════════════════════════════════════════════════════════
- 📌 ملخص توجيهي | Guiding Summary
- ═══════════════════════════════════════════════════════════════════════════
- 📄 الملف: TODONext_19_08_2026.md
- 📂 المسار: TODONext_19_08_2026.md
- 🎯 الهدف الرئيسي: خطة التنفيذ الموزعة على الحزم مع التنبيهات وأوامر التشغيل
- 📋 المعايير:
- - الالتزام بقواعد AGENTS.md الصارمة (ثيم فاتح نقي، تفاعل ماوس حصري).
- - حدود الكود: 400 سطر/ملف، 50 سطر/دالة.
- - اختبارات >= 95% تغطية.
- 🏷️ المعرف: DOC-PLAN-02
- 📅 تاريخ الإنشاء: 2026-08-19
- 🔄 آخر تحديث: 2026-08-20 (توزيع على الحزم + تحديث أوامر التنفيذ)
- 👤 المالك: Hossam El-Din Abdel-Moaty El-Khouly - All rights reserved
- ⚖️ الترخيص: MIT License
- ═══════════════════════════════════════════════════════════════════════════
  */

# 📋 خطة التنفيذ الموزعة — Distributed Execution Plan

## LibreText Editor Suite — 2026-08-20

---

## 📊 الحالة الراهنة (Current Status)

| الحزمة                   | المنجز  | المتبقي                                | الأولوية |
| ------------------------ | ------- | -------------------------------------- | -------- |
| `@libretext/core`        | ✅ 100% | —                                      | —        |
| `@libretext/algorithms`  | ✅ 90%  | HLOOKUP, PMT/NPV/IRR, Topological Sort | عالية    |
| `@libretext/storage`     | ✅ 80%  | IndexedDBStore v2, SnapshotManager v2  | عالية    |
| `@libretext/templates`   | ✅ 70%  | 10 قوالب للمجالات الأربعة              | متوسطة   |
| `@libretext/serializers` | ✅ 80%  | تحسينات PDF/LaTeX                      | منخفضة   |
| `@libretext/plugins`     | ✅ 75%  | تكامل Mermaid/KaTeX                    | منخفضة   |
| `@libretext/adapters`    | ✅ 85%  | مزامنة تخزين/ماوس                      | متوسطة   |
| UI Components            | ❌ 0%   | FloatingGizmo, ContextMenu, WheelZoom  | عالية    |

---

## 🧮 packages/algorithms — المراحل المتبقية

### المرحلة 1: دوال البحث المتقدمة (ALGO-018)

| المعرف   | المهمة                                                     | الحالة  | التنبيهات                                                                                         |
| -------- | ---------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------- |
| ALGO-018 | `HLOOKUP(lookupValue, tableArray, rowIndex, rangeLookup?)` | ⏳ جديد | ⚠️ يجب أن يتوافق مع VLOOKUP في التعامل مع الأنواع. استخدم `flatten()` من functions-lookup-date.ts |
| ALGO-018 | `PMT(rate, nper, pv, fv?, type?)`                          | ⏳ جديد | ⚠️ الدوال المالية تحتاج دقة حسابية عالية. استخدم `Number.EPSILON` للفحص                           |
| ALGO-018 | `NPV(rate, ...values)`                                     | ⏳ جديد | ⚠️ يجب دعم المصفوفات المتداخلة عبر `flatten()`                                                    |
| ALGO-018 | `IRR(values, guess?)`                                      | ⏳ جديد | ⚠️ خوارزمية Newton-Raphson مع حد أقصى 100 تكرار لمنع الحلقات اللانهائية                           |

**أمر التنفيذ:**

```bash
# إنشاء ملف functions-financial.ts
touch packages/algorithms/src/formula/functions-financial.ts
# كتابة اختبارات
touch packages/algorithms/tests/formula/functions-financial.test.ts
# تحديث barrel export
# تحديث FUNCTION_INDEX.md
```

### المرحلة 2: خوارزمية كشف الحلقات (ALGO-019)

| المعرف   | المهمة                                      | الحالة  | التنبيهات                                                          |
| -------- | ------------------------------------------- | ------- | ------------------------------------------------------------------ |
| ALGO-019 | `buildDependencyGraph(cells)`               | ⏳ جديد | ⚠️ يجب أن يكون O(V+E) تعقيداً. استخدم adjacency list               |
| ALGO-019 | `detectCycle(graph)`                        | ⏳ جديد | ⚠️ DFS مع 3 ألوان (أبيض/رمادي/أسود). رمي `#CIRCULAR!` عند كشف حلقة |
| ALGO-019 | `topologicalSort(graph)`                    | ⏳ جديد | ⚠️ Kahn's Algorithm مع queue. فحص أن النتيجة تغطي كل العقد         |
| ALGO-019 | `getRecalculationOrder(graph, changedCell)` | ⏳ جديد | ⚠️ BFS من الخلية المعدلة فقط (لا إعادة حساب كل شيء)                |

**أمر التنفيذ:**

```bash
# إنشاء ملف dependency-graph.ts
touch packages/algorithms/src/formula/dependency-graph.ts
# كتابة اختبارات
touch packages/algorithms/tests/formula/dependency-graph.test.ts
```

### المرحلة 3: كشف التصادم الهندسي (ALGO-020)

| المعرف   | المهمة                                             | الحالة  | التنبيهات                                                     |
| -------- | -------------------------------------------------- | ------- | ------------------------------------------------------------- |
| ALGO-020 | `checkCollision(rectA, rectB)`                     | ⏳ جديد | ⚠️ AABB collision detection. يجب أن يكون O(1)                 |
| ALGO-020 | `getIntersectionArea(rectA, rectB)`                | ⏳ جديد | ⚠️ لحساب نسبة التداخل للتحديد التلقائي                        |
| ALGO-020 | `snapToNearestElement(point, elements, threshold)` | ⏳ جديد | ⚠️ جذب مagnetique مع عتبة قصوى. لا تُجبر العناصر على الالتصاق |

**أمر التنفيذ:**

```bash
touch packages/algorithms/src/spatial/collision.ts
touch packages/algorithms/tests/spatial/collision.test.ts
```

---

## 💾 packages/storage — المراحل المتبقية

### المرحلة 4: IndexedDBStore v2 (STORE-013)

| المعرف    | المهمة                                            | الحالة   | التنبيهات                                                                |
| --------- | ------------------------------------------------- | -------- | ------------------------------------------------------------------------ |
| STORE-013 | إعادة كتابة `indexeddb.ts` ليتوافق مع `Store<T>`  | 🔧 تحسين | ⚠️ الكود الحالي يفتح DB في كل عملية. يجب **حفظ الاتصال** وإعادة استخدامه |
| STORE-013 | دعم `getEntry()`, `entries()`, `keys()`, `size()` | ⏳ جديد  | ⚠️ استخدم `getAllKeys()` من IDBObjectStore                               |
| STORE-013 | نظام الأحداث `on(eventType, listener)`            | ⏳ جديد  | ⚠️ حافظ على cleanup لمنع memory leaks                                    |
| STORE-013 | Schema Migration عند تغيير الإصدار                | ⏳ جديد  | ⚠️ استخدم `onupgradeneeded` مع versioning                                |
| STORE-013 | `structuredClone` بدلاً من `JSON.parse/stringify` | 🔧 تحسين | ⚠️ تجنب فقدان الأنواع المركبة (Date, RegExp)                             |

**أمر التنفيذ:**

```bash
# إعادة كتابة indexeddb.ts بالكامل
# كتابة اختبارات شاملة
touch packages/storage/tests/indexeddb-v2.test.ts
```

### المرحلة 5: SnapshotManager v2 (STORE-014)

| المعرف    | المهمة                            | الحالة   | التنبيهات                                                       |
| --------- | --------------------------------- | -------- | --------------------------------------------------------------- |
| STORE-014 | ضغط الفروقات (Delta Snapshots)    | ⏳ جديد  | ⚠️ JSON Patch (RFC 6902) بدلاً من نسخ كامل. وفر 60%+ من الذاكرة |
| STORE-014 | `getSnapshot(id)` للوصول التاريخي | ⏳ جديد  | ⚠️ فهرسة بالوقت أو بالرقم التسلسلي                              |
| STORE-014 | `getHistory()` مع metadata كامل   | 🔧 تحسين | ⚠️ أضف `description` و `operationType` لكل لقطة                 |
| STORE-014 | تصدير/استيراد JSON                | ⏳ جديد  | ⚠️ ضغط LZString للتصدير                                         |

**أمر التنفيذ:**

```bash
# إعادة كتابة snapshots.ts مع Delta Snapshots
# كتابة اختبارات
touch packages/storage/tests/snapshots-v2.test.ts
```

---

## 🎨 packages/templates — القوالب المطلوبة

### المرحلة 6: قوالب المجالات الأربعة (TPL-001..010)

| المعرف  | القالب               | المجال  | التنبيهات                                                                 |
| ------- | -------------------- | ------- | ------------------------------------------------------------------------- |
| TPL-001 | `formal-letter`      | Writer  | ⚠️ خطاب رسمي ثنائي اللغة (عربي/إنجليزي). يجب أن يحتوي header وfooter      |
| TPL-002 | `technical-report`   | Writer  | ⚠️ تقرير تقني مع جداول ومراجع. استخدم `heading()` و `codeBlock()` من core |
| TPL-003 | `meeting-minutes`    | Writer  | ⚠️ محضر اجتماع مع قائمة مهام (checkboxes)                                 |
| TPL-004 | `budget-planner`     | Calc    | ⚠️ ميزانية مع SUM, IF, AVERAGE. تحقق من صحة الصيغ قبل التسليم             |
| TPL-005 | `invoice-generator`  | Calc    | ⚠️ فاتورة مع حساب الضريبة (15% VAT). استخدم DATEDIF للتواريخ              |
| TPL-006 | `student-gradebook`  | Calc    | ⚠️ سجل درجات مع MIN, MAX, AVERAGE, MEDIAN                                 |
| TPL-007 | `pitch-deck`         | Impress | ⚠️ عرض 16:9 مع أبعاد `LogicalCoordinate` بالسنتيمتر                       |
| TPL-008 | `educational-slides` | Impress | ⚠️ شرائح تدريبية مع مربعات وسائط                                          |
| TPL-009 | `inventory-tracker`  | Base    | ⚠️ سجل مخزون مع حقول نصية ورقمية                                          |
| TPL-010 | `contacts-crm`       | Base    | ⚠️ سجل عملاء مع حقول التاريخ                                              |

**أمر التنفيذ:**

```bash
# إنشاء ملفات القوالب
for dir in writer calc impress base; do
  mkdir -p packages/templates/src/$dir
done
# كتابة اختبارات لكل قالب
touch packages/templates/tests/templates.test.ts
```

---

## 🧩 packages/serializers — التحسينات

### المرحلة 7: تحسينات المحولات (SER-006..008)

| المعرف  | المهمة           | الحالة   | التنبيهات                                                             |
| ------- | ---------------- | -------- | --------------------------------------------------------------------- |
| SER-006 | تحسين محول PDF   | 🔧 تحسين | ⚠️ ترقيم صفحات + هوامش دقيقة. لا تستخدم مكتبات خارجية (ابقَ zero-dep) |
| SER-007 | تحسين محول LaTeX | 🔧 تحسين | ⚠️ دعم المعادلات الرياضية `\frac{}{}`, `\sum_{i=0}^{n}`               |
| SER-008 | تحسين محول HTML  | 🔧 تحسين | ⚠️ تنظيف XSS عبر regex (لا DOMPurify في zero-dep)                     |

**أمر التنفيذ:**

```bash
# تعديل الملفات الموجودة
# تشغيل اختبارات موجودة
pnpm test -- --filter=@libretext/serializers
```

---

## 🔌 packages/plugins — التكامل

### المرحلة 8: تحسين الإضافات (PLUG-003..004)

| المعرف   | المهمة                 | الحالة   | التنبيهات                                             |
| -------- | ---------------------- | -------- | ----------------------------------------------------- |
| PLUG-003 | تكامل Mermaid الرسومية | 🔧 تحسين | ⚠️ دعم المخططات الانسيابية (flowchart) + مخططات Gantt |
| PLUG-004 | تحسين KaTeX/Math       | 🔧 تحسين | ⚠️ عرض المعادلات المضمنة `$...$` والمنفصلة `$$...$$`  |

**أمر التنفيذ:**

```bash
# تعديل ملفات موجودة
pnpm test -- --filter=@libretext/plugins
```

---

## 🖱️ واجهة المستخدم — المكونات البصرية

### المرحلة 9: مكونات التفاعل بالماوس (UI-001..004)

| المعرف | المهمة                 | الحالة  | التنبيهات                                                                                |
| ------ | ---------------------- | ------- | ---------------------------------------------------------------------------------------- |
| UI-001 | `FloatingGizmo.tsx`    | ⏳ جديد | ⚠️ 8 نقاط تحكم (NW/N/NE/E/SE/S/SW/W) + مقبض تدوير. **فأرة فقط** — لا اختصارات            |
| UI-002 | `ContextMenu.tsx`      | ⏳ جديد | ⚠️ قوائم مخصصة لكل مجال: Writer (تنسيق), Calc (إدراج/دمج), Impress (طبقات), Base (سجلات) |
| UI-003 | `WheelZoom.tsx`        | ⏳ جديد | ⚠️ تثبيت مركز التكبير عند مؤشر الفأرة. `event.preventDefault()` على wheel                |
| UI-004 | `WhiteboardCanvas.tsx` | ⏳ جديد | ⚠️ سبورة بيضاء نقية مع `bg-white`. **بدون أي عنصر واجهة على السبورة** — فارغة تماماً     |

**أمر التنفيذ:**

```bash
# إنشاء مكونات UI (يجب أن تكون في packages/playground أو packages/adapters)
# ملاحظة: هذه المكونات تحتاج React/Vue — تأكد من وجود_adapter_
```

### المرحلة 10: ثيمات السبورة البيضاء (UI-005)

| المعرف | المهمة            | الحالة  | التنبيهات                                                                                                  |
| ------ | ----------------- | ------- | ---------------------------------------------------------------------------------------------------------- |
| UI-005 | 6 ثيمات فاتحة فقط | ⏳ جديد | ⚠️ **ممنوع** أي لون داكن. الأسماء: Crisp White, Nordic Sky, Soft Ivory, Warm Sand, Fresh Linen, Mist Pearl |

**أمر التنفيذ:**

```bash
# إنشاء ملف ثيمات
touch packages/playground/src/themes.ts
```

---

## 📇 التوثيق الفوري

### المرحلة 11: تحديث الفهارس (DOC-010)

| المعرف  | المهمة                         | الحالة  | التنبيهات                                            |
| ------- | ------------------------------ | ------- | ---------------------------------------------------- |
| DOC-010 | تحديث `FUNCTION_INDEX.md`      | ⏳ جديد | ⚠️ يجب تحديثه بعد كل دالة جديدة. احفظ العدد التسلسلي |
| DOC-010 | تحديث `Components Registry.md` | ⏳ جديد | ⚠️ سجّل كل مكون UI جديد                              |
| DOC-010 | تحديث `CHANGELOG.md`           | ⏳ جديد | ⚠️ وثّق كل تغيير بتنسيق Keep a Changelog             |
| DOC-010 | تحديث `JOURNAL.md`             | ⏳ جديد | ⚠️ سجّل التاريخ والقرارات المعمارية                  |

---

## 🔄 أوامر التنفيذ الموحدة

### أمر تنفيذ شامل (Run All):

```bash
cd /home/sam2/Projects/libretext-editor-suite

# 1. التحقق النوعي
pnpm typecheck

# 2. الاختبارات
pnpm test

# 3. التنسيق
pnpm lint

# 4. بناء جميع الحزم
pnpm build
```

### أمر تنفيذ مرحلتي (Phase by Phase):

```bash
# المرحلة 1-3: algorithms
pnpm test -- --filter=@libretext/algorithms

# المرحلة 4-5: storage
pnpm test -- --filter=@libretext/storage

# المرحلة 6: templates
pnpm test -- --filter=@libretext/templates

# المرحلة 7-8: serializers + plugins
pnpm test -- --filter=@libretext/serializers --filter=@libretext/plugins
```

---

## ⚠️ التنبيهات العامة (Global Warnings)

1. **🚫 ممنوع حذف كود موجود:** عند تحسين ملف، احفظ الكود الحالي في نسخة احتياطية أولاً.
2. **🎨 ثيم فاتح نقي حصراً:** أي لون داكن = خطأ فادح. `bg-white` أو `bg-slate-50` فقط.
3. **🖱️ تفاعل ماوس فقط:** لا اختصارات لوحة مفاتيح كوسيلة تفاعل رئيسية.
4. **📏 حدود الكود:** 400 سطر/ملف، 50 سطر/دالة. تقسيم إلزامي عند التجاوز.
5. **🧪 اختبارات قبل commit:** `pnpm typecheck && pnpm test && pnpm lint` إلزامي.
6. **📝 ترويسة إلزامية:** كل ملف يجب أن يحتوي الترويسة التوجيهية بالعربية.
7. **🔒 صفر اعتماديات خارجية:** لا import من مكتبات خارجية في core/algorithms.
8. **📊 تحديث FUNCTION_INDEX.md:** بعد كل دالة جديدة.

---

## 📅 الجدول الزمني المقترح

| الأسبوع | المراحل                      | النتيجة المتوقعة              |
| ------- | ---------------------------- | ----------------------------- |
| 1       | ALGO-018 + ALGO-019          | دوال مالية + كشف حلقات        |
| 2       | ALGO-020 + STORE-013         | كشف تصادم + IndexedDB v2      |
| 3       | STORE-014 + TPL-001..010     | SnapshotManager v2 + 10 قوالب |
| 4       | SER-006..008 + PLUG-003..004 | تحسينات محولات + إضافات       |
| 5       | UI-001..005                  | مكونات التفاعل بالماوس        |
| 6       | DOC-010 + Final Testing      | توثيق + اختبارات نهائية       |

---

تنبهات مهمة:

_تم إعداد هذه الخطة كدليل تنفيذي موزع مع أوامر تشغيل جاهزة وتنبيهات لكل بند._
