/**

- ============================================================
- 📄 الملف: CONTRIBUTING.md
- 📂 المسار: CONTRIBUTING.md
- 🎯 الهدف الرئيسي: دليل شامل للمساهمين في المشروع، يتضمن
- كيفية الإعداد و准则 الكود وعملية Pull Request.
- 📋 المعايير:
- - يجب أن يكون الدليل واضحًا ومختصرًا.
- - يجب أن يحتوي على خطوات الإعداد والاختبار.
- - يجب أن يشرح عملية Pull Request كاملة.
- 🧪 الاختبارات: لا توجد اختبارات (ملف إداري).
- 🏷️ المعرف: DOC-GUIDE-01
- 📅 تاريخ الإنشاء: 2026-08-19
- 👤 المالك: Hossam El-Din Abdel-Moaty El-Khouly - All rights reserved
- ⚖️ الترخيص: MIT License
- 📚 المصادر المقتبسة: لا توجد.
- ============================================================
  */

# دليل المساهمة في LibreText Editor Suite

شكراً لاهتمامك بالمساهمة في هذا المشروع! هذا الدليل سيساعدك على البدء.

## الاستعداد للتطوير

### المتطلبات

- Node.js >= 18
- pnpm >= 8
- Git

### خطوات الإعداد

1. استنساخ المستودع:

```bash
git clone https://github.com/[your-org]/libretext-editor-suite.git
cd libretext-editor-suite
```

2. تثبيت الاعتماديات:

```bash
pnpm install
```

3. تشغيل الاختبارات للتأكد من أن كل شيء يعمل:

```bash
pnpm test
```

## قواعد الكود

### TypeScript

- يجب استخدام TypeScript الصارم (strict mode).
- يجب توثيق جميع الدوال والأنواع بتعليقات JSDoc.
- يجب استخدام `readonly` للخصائص التي لا تتغير.

### الملفات

- يجب إضافة الترويسة العربية إلى أعلى كل ملف (انظر البروميت في `مخطط أولي.txt`).
- يجب استخدام المعرف الفريد (ID) لكل ملف وفق فهرسة المشروع.

### الاختبارات

- يجب كتابة اختبارات لكل وظيفة جديدة.
- يجب أن تكون تغطية الاختبارات >= 95%.
- يجب تشغيل `pnpm test` قبل إرسال Pull Request.

### التنسيق

- يجب استخدام Prettier لتنسيق الكود.
- يجب استخدام ESLint للتحقق من الكود.
- يمكن تشغيل التنسيق التلقائي عبر:

```bash
pnpm format
pnpm lint
```

## عملية Pull Request

1. إنشاء فرع جديد من `main`:

```bash
git checkout -b feature/your-feature-name
```

2. إجراء التغييرات المطلوبة.
3. تشغيل الاختبارات والتنسيق:

```bash
pnpm test
pnpm format
pnpm lint
```

4. إرسال Pull Request مع وصف واضح للتغييرات.
5. انتظار المراجعة والموافقة.

## التقارير والمشاكل

- يرجى استخدام Issues Templates المتاحة.
- يرجى تقديم تفاصيل كافية عند الإبلاغ عن خلل.

## الترخيص

بالمساهمة في هذا المشروع، أنت توافق على أن عملك سيُنشر تحت ترخيص MIT.

---

## 🤖 DevStudio — الشريك الذكي

**DevStudio** هو المساعد التلقائي للمشروع. ينفذ المهام الروتينية ويوفر وقتك.

### أوامر DevStudio

```bash
pnpm devstudio scan          # مسح المشروع + حفظ لقطة
pnpm devstudio scan --test   # مسح + اختبارات
pnpm devstudio status        # عرض حالة المشروع
pnpm devstudio help          # جميع الأوامر
```

### سير العمل المقترح

```bash
# 1. قبل البدء
pnpm devstudio scan

# 2. نفّذ التعديلات

# 3. بعد التعديلات
pnpm devstudio scan
pnpm test
pnpm devstudio status
```

### القاعدة

> **أي مهمة تتكرر مرتين → DevStudio يتولاها.**
> **المطور يركز على القرارات المعمارية فقط.**

详细 في `DEVSTUDIO_PLAN.md`.
