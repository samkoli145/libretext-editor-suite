/**
 * ═══════════════════════════════════════════════════════════════════════════
 * 📌 ملخص توجيهي | Guiding Summary
 * ═══════════════════════════════════════════════════════════════════════════
 * 📄 الملف: DESIGN_BOOK.md
 * 📂 المسار: DESIGN_BOOK.md
 * 🎯 الهدف الرئيسي: المرجع القياسي الشامل وكتاب التصميم المعماري (Design Book & Spec)
 *    لمكتبة LibreText Editor Suite. يوجه المصممين ومطوري الواجهات لبناء واجهات
 *    انتقائية متناسقة، ويحدد مقاسات ومعايير وخصائص كل كتلة (Block) وأداة (Tool)
 *    وقائمة سياقية وجيزمو عائم وفق فلسفة السبورة البيضاء والثيم الفاتح النقي.
 * 📋 المعايير:
 *    - الالتزام التام بقواعد الثيم الفاتح النقي (Pure Daylight Canvas).
 *    - تفاعل الفأرة الكامل (Mouse-First & Right-Click Context Menus).
 *    - المعايير الهندسية والنسب الرياضية للمقاسات والمحاذاة.
 * 🏷️ المعرف: DOC-ADMIN-13
 * 📅 تاريخ الإنشاء: 2026-08-21
 * ═══════════════════════════════════════════════════════════════════════════
 * 👤 المالك: Hossam El-Din Abdel-Moaty El-Khouly - All rights reserved
 * ⚖️ الترخيص: MIT License
 * ═══════════════════════════════════════════════════════════════════════════
 */

# 📖 كتاب التصميم والمعايير القياسية | LibreText Design Book & UI Specification

مرحباً بك في **كتاب التصميم القياسي لمكتبة LibreText Suite**. تم إعداد هذا الدليل ليكون المرجع الشامل والأعلى لأي مصمم أو مهندس واجهات (UI/UX Architect) يقوم ببناء واجهات مستخدم مخصصة أو دمج كتل وأدوات المكتبة في تطبيقاته.

---

## 🏛️ 1. الفلسفة المعمارية للتصميم (Design Philosophy)

مكتبة LibreText تقوم على 3 مبادئ تصميمية غير قابلة للتفاوض:

1. **السبورة البيضاء اللانهائية (Pure Daylight Canvas):**
   - بيئة العمل الافتراضية والوحيدة هي بيئة النهار الفاتحة عالية التباين، مريحة للعين، وخالية من التشويش البصري.
   - حظر كامل للثيمات الداكنة (Dark Themes) لضمان دقة القراءة والطباعة والتصدير المطابق للأصل (WYSIWYG True-Print).
2. **الواجهة الانتقائية النمطية (Selective & Composable UI):**
   - النظام لا يفرض واجهة محددة، بل يقدم "كتلاً معيارية" (Blocks) و"أدوات تحكم" (Tools) و"أشرطة عائمة" (Gizmos) يمكن للمصمم انتقاؤها وتركيبها بمرونة تامة.
3. **أولوية التفاعل بالفأرة (Mouse-First Interaction):**
   - كل أداة أو كتلة يجب أن تكون قابلة للإدارة بنسبة 100% بالماوس (النقر بالزر الأيمن، السحب والإفلات، التمرير، والجيزمو المباشر) دون إجبار المستخدم على حفظ اختصارات لوحة المفاتيح.

---

## 🎨 2. منظومة الألوان الفاتحة (The 6 Daylight Themes Palette)

يمنع استخدام الألوان الداكنة كخلفيات. لوحة الألوان المعتمدة تتكون من 6 سمات نهارية فاتحة نقية:

| اسم السمة (Theme) | لون الخلفية (Canvas) | لون السطح (Surface) | لون الحدود (Border) | لون النص الأساسي (Text) | لون التمييز (Accent) |
|---|---|---|---|---|---|
| **Crisp White (الافتراضي)** | `#FFFFFF` | `#F8FAFC` | `#E2E8F0` | `#0F172A` | `#2563EB` (Royal Blue) |
| **Nordic Sky** | `#F0F4F8` | `#FFFFFF` | `#D9E2EC` | `#102A43` | `#0284C7` (Sky Cyan) |
| **Soft Ivory** | `#FCFBF7` | `#FFFFFF` | `#EBE8DF` | `#2D2B2A` | `#D97706` (Warm Amber) |
| **Warm Sand** | `#FBF9F5` | `#F3EFE6` | `#E3DDD0` | `#38332E` | `#C2410C` (Terracotta) |
| **Fresh Linen** | `#F9FAF8` | `#FFFFFF` | `#DEE2DC` | `#1A241B` | `#059669` (Emerald Green) |
| **Mist Pearl** | `#F8F9FA` | `#EDF0F2` | `#DCE1E5` | `#212529` | `#6366F1` (Indigo Pearl) |

---

## 📐 3. القواعد القياسية للمقاسات والمحاذاة (Geometry & Sizing System)

تتبع المكتبة شبكة رياضية بنظام 4px / 8px Base Grid:

* **الهوامش والحواشي (Spacing Tokens):**
  - `xs`: 4px | `sm`: 8px | `md`: 12px | `lg`: 16px | `xl`: 24px | `2xl`: 32px
* **نصف قطر الحواف (Border Radius Hierarchy):**
  - الحاويات الكبيرة واللوحات: `12px` إلى `16px`
  - الأزرار وحقول الإدخال والوسوم: `6px` إلى `8px`
  - الرقائق والعلامات (Badges / Pills): `9999px` (Fully rounded)
  - **قاعدة الحواف المتداخلة الرياضية:** `Inner Radius = Outer Radius - Padding`.
* **الظلال والعمق البصري (Elevation & Shadows):**
  - الطبقة 1 (العناصر على السبورة): `0 1px 3px 0 rgba(0, 0, 0, 0.05)`
  - الطبقة 2 (القوائم السياقية والأشرطة العائمة): `0 4px 6px -1px rgba(0, 0, 0, 0.07), 0 2px 4px -1px rgba(0, 0, 0, 0.04)`
  - الطبقة 3 (النوافذ المنبثقة ومربعات الحوار): `0 20px 25px -5px rgba(0, 0, 0, 0.08), 0 10px 10px -5px rgba(0, 0, 0, 0.03)`

---

## 🧱 4. دليل الكتل المعمارية والمعاملة الخاصة لكل كتلة (Block Catalog & Specs)

كل كتلة (Block) في المكتبة لها طبيعة بيانية ومكانية خاصة، ويجب على المصمم مراعاة معاييرها الخاصة كالتالي:

```
┌────────────────────────────────────────────────────────────────────────┐
│                        دليل كتل محتوى المحرر                           │
├───────────────────┬──────────────────────┬─────────────────────────────┤
│ الكتلة (Block)    │ المقاسات الموصى بها   │ السلوك والشروط الخاصة       │
├───────────────────┼──────────────────────┼─────────────────────────────┤
│ 📝 الفقرة والنصوص │ Max-width: 65-75ch   │ line-height: 1.6-1.7        │
│ 📊 الجداول الحسابية│ Min-col: 80px        │ Virtualized Grid + A1 bar   │
│ 📐 المتجهات والرسم│ 1:1 Aspect or Vector │ Snap to Guides + Gizmo      │
│ 🧪 الكراس الحوسبي │ Full container width │ Unit calculation scratchpad │
│ 🖼️ الصور والوسائط │ Scalable w/ Lock Ratio│ Matte filter + Aspect handle│
│ 💻 استوديو الكود  │ Monospace font 14px  │ Line numbers + Run button   │
│ 🎓 المربع الأكاديمي│ Margin: 16px 0       │ Left indicator 4px (solid)  │
└───────────────────┴──────────────────────┴─────────────────────────────┘
```

### 1. كتلة النص والفقرات (`ParagraphBlock` / `HeadingBlock`)
- **الهدف:** القراءة المريحة وتحرير النصوص الغنية.
- **المقاسات الموصى بها:** أقصى عرض للسطر `65ch` إلى `75ch` لمنع إجهاد العين. حجم الخط الأساسي: `16px`، ارتفاع السطر `1.6` إلى `1.7`.
- **المعاملة الخاصة:** 
  - دعم التحرير المباشر الموضعي (Inline Editing).
  - تحديد النص يظهر تلقائياً شريط التنسيق المصغر (Inline Floating Gizmo).

### 2. كتلة الجداول الحسابية (`SpreadsheetGridBlock`)
- **الهدف:** جداول بيانات ذكية بصيغ رياضية (A1 Notation).
- **المقاسات الموصى بها:** عرض الخلية الافتراضي `100px`، ارتفاع الصف الافتراضي `28px`، شريط الصيغ بارتفاع `36px`.
- **المعاملة الخاصة:**
  - رؤوس الأعمدة (`A, B, C...`) ورؤوس الصفوف (`1, 2, 3...`) ثابتة أثناء التمرير (Sticky Headers).
  - تفعيل التمرير الافتراضي (Virtual Scrolling) للجداول الضخمة.
  - النقر بالزر الأيمن يفتح قائمة عمليات الجدول (إدراج صف، حذف عمود، دمج خلايا، تنسيق الأرقام والعملات).

### 3. كتلة المتجهات والرسومات (`VectorCanvasBlock`)
- **الهدف:** رسم مسارات بيزييه، أشكال هندسية، ومخططات توجيهية.
- **المقاسات الموصى بها:** خطوط الشبكة المغناطيسية `8px`، أبعاد مقابض التحكم (Control Handles) بحجم `8x8px`.
- **المعاملة الخاصة:**
  - إظهار خطوط المحاذاة الذكية الزرقاء (Alignment Guides) عند محاذاة العناصر.
  - دعم السحب، التدوير، والتحجيم مع الحفاظ على النسبة (Shift + Drag).

### 4. كتلة الكراس الحوسبي (`ComputationalScratchpadBlock`)
- **الهدف:** إجراء عمليات حسابية فورية وحسابات وحدات فيزيائية ومالية متسلسلة.
- **المقاسات الموصى بها:** حاوية مرنة بارتفاع أدنى `200px` مقسمة إلى قسم الإدخال وقسم النتائج الفورية.
- **المعاملة الخاصة:** تظليل النتائج بألوان واضحة وتحديث التقييم لحظياً مع كل نقرة فأرة أو تعديل رقم.

### 5. كتلة استوديو الكود والبرمجة (`InteractiveCodeStudioBlock`)
- **الهدف:** عرض وتحرير وتشغيل مقتطفات برمجية بلغات متعددة.
- **المقاسات الموصى بها:** خط برمجيات أحادي المسافة (`JetBrains Mono` أو `Fira Code`) بحجم `13px` أو `14px`.
- **المعاملة الخاصة:** ترقيم الأسطر على اليسار، شريط علوي يحتوي منتقي اللغة وزر التشغيل الفوري.

### 6. كتلة الصور والوسائط (`FloatingImageBlock` / `MediaStudio`)
- **الهدف:** إدراج الصور ومعالجتها وتطبيق الفلاتر وعزل الخلفية.
- **المقاسات الموصى بها:** الحفاظ على نسبة العرض إلى الارتفاع الأصلية (Aspect Ratio) مع مقابض تحجيم في الزوايا الـ 4.
- **المعاملة الخاصة:**
  - النقر يظهر جيزمو التحكم في دوران الصورة والقص والفلاتر.
  - القائمة السياقية توفر خيارات التصدير بصيغ متعددة (PNG, WebP, SVG).

### 7. كتلة الصناديق الأكاديمية والملاحظات (`AcademicCalloutBlock`)
- **الهدف:** إبراز الملاحظات، التحذيرات، النظريات، والمعادلات.
- **المقاسات الموصى بها:** هامش علوي وسفلي `16px`، حشوة داخلية `16px`، شريط جانبي ملون بسماكة `4px`.
- **المعاملة الخاصة:** استخدام ألوان هادئة فاتحة متوافقة مع خلفية المستند (مثل الأزرق الفاتح للملاحظة، والأخضر للنظرية، والكهرماني للتحذير).

---

## 🛠️ 5. معايير أدوات التحكم والشريط العلوي والقوائم (Tooling & Controls Spec)

### 1. الشريط العلوي الرئيسي (`SharedRibbonBar`):
- **الارتفاع:** `44px` للشريط المضغوط، أو `88px` للشريط المفصل متعدد التبويبات.
- **التصميم:** خلفية `bg-white` مع خط فاصل سفلي `border-b border-slate-200`.
- **الأزرار:** أبعاد التفاعل لا تقل عن `32x32px` مع هوفر ناعم `hover:bg-slate-100` وحواف دائرية `rounded-md`.

### 2. القائمة السياقية الشاملة للزر الأيمن (`SharedContextMenu`):
- **العرض:** أدنى عرض `180px`، أقصى عرض `260px`.
- **العناصر:** ارتفاع العنصر `32px`، يتضمن أيقونة بحجم `16x16px` على اليمين/اليسار، والنص، واختصار لوحة المفاتيح المعروض كمعلومة إرشادية فقط.
- **القواعد:** تظهر دائماً بجانب مؤشر الفأرة مع تصحيح تلقائي إذا كانت قريبة من حافة الشاشة (Edge Clamping).

### 3. الجيزمو العائم (`FloatingGizmoEngine`):
- **الموضع:** يعلو العنصر المحدد بمسافة `8px` ومثبت في المنتصف.
- **التصميم:** كبسولة بيضاء أنيقة `bg-white/95 backdrop-blur-sm shadow-lg border border-slate-200 rounded-full px-2 py-1`.
- **الاستجابة:** يختفي تلقائياً عند إلغاء التحديد.

---

## 🧩 6. كيفية بناء واجهة انتقائية (How to Build a Custom Selective UI)

إذا كنت تصمم واجهة مخصصة لمشروعك، اتبع نمط التكوين التركيبي الموصى به:

```tsx
import React from 'react';
import { 
  SharedRibbonBar, 
  SharedContextMenu, 
  SharedOutlinePanel, 
  UnifiedRichTextAndMathEditor 
} from '@libretext/adapters';

export function MySelectiveCustomEditor() {
  return (
    <div className="flex flex-col h-screen bg-slate-50 text-slate-900 select-none">
      {/* 1. الشريط العلوي الانتقائي */}
      <SharedRibbonBar 
        activeTab="home"
        showMathTools={true}
        showGridTools={false} 
      />

      <div className="flex flex-1 overflow-hidden relative">
        {/* 2. اللوحة الجانبية لفهرس المستند (اختياري) */}
        <aside className="w-64 border-r border-slate-200 bg-white">
          <SharedOutlinePanel />
        </aside>

        {/* 3. مساحة العمل والسبورة البيضاء */}
        <main className="flex-1 overflow-auto p-8 flex justify-center bg-slate-100/60">
          <div className="w-full max-w-4xl bg-white shadow-sm border border-slate-200 rounded-xl p-12 min-h-[1000px]">
            <UnifiedRichTextAndMathEditor />
          </div>
        </main>

        {/* 4. القائمة السياقية الموحدة للزر الأيمن */}
        <SharedContextMenu />
      </div>
    </div>
  );
}
```

---

## 📋 7. قائمة التحقق للمصممين الجدد (New Designer Checklist)

قبل اعتماد أي واجهة أو كتلة جديدة في المشروع، تأكد من:
- [ ] هل الخلفية فاتحة تماماً وتتبع إحدى سمات النهار الست المعتمدة؟
- [ ] هل يمكن تنفيذ كافة إجراءات الكتلة بالفأرة بنسبة 100% (بما في ذلك الزر الأيمن)؟
- [ ] هل تم ضبط المقاسات والهوامش وفق شبكة الـ 4px / 8px؟
- [ ] هل خطوط النصوص متناسقة وعرض السطور لا يتجاوز `75ch`؟
- [ ] هل القوائم والجيزموهات تحترم حدود الشاشة (Edge Clamping) ولا تختفي تحت الحواف؟
- [ ] هل تمت إضافة الترويسة الإلزامية وتوثيق المكون في `Components Registry.md`؟

---
*تم إعداد هذا الدليل لحفظ الجهد الهندسي وتوحيد الرؤية البصرية والمعمارية لجميع المساهمين والمصممين في مشروع LibreText Editor Suite.*
